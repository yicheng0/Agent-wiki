---
author: 执着的猫
source: 微信公众号
url: https://mp.weixin.qq.com/s?__biz=MzY4NjMwMTQzNw==&mid=2247483675&idx=1&sn=efb510ed7a6fecf2c9444dffa4c8d50b&chksm=f2b3752697a3c93a3644a0c1a96fa1e127de8b7e16291d81a5253d12783a29d5d4d896c1dce5&mpshare=1&scene=1&srcid=0530UICrR6eZAGlUGWUN41PP&sharer_shareinfo=19524bef59f1ee23ab4e93810bcdac53&sharer_shareinfo_first=95f9313d2434c706ee06c2c5ed99f818#rd
saved: 2026-05-30 00:18:15
tags:
  - 笔记同步助手
id: 49d91b15-efbd-4886-97db-131b94384ea9
---

公众号名称：猫的AI安全笔记

作者名称：执着的猫

发布时间：2026-05-29 23:59

  

关于 Claude Code 封号，社区里已经有很多经验总结：固定 IP、避开机房段、对齐时区和语言、不要多设备同时活跃、不要长时间高并发请求。它们并非完全没有道理，但如果只盯着这些单点信号，很容易误判风险的来源。

本文不再给出另一份“防封清单”，而是换一个角度：回到 Claude Code 客户端本身，看看它在正常使用过程中会向服务端发送哪些信息。

这件事的意义在于，服务端看到的并不一定只是一条孤立的推理请求。围绕一次请求，客户端还会带上账号、设备、进程、工作目录、git 仓库和运行环境等上下文。理解这些上下文，比记住某几条经验规则更有助于判断风险。

先把边界说清楚：本文基于 Claude Code v2.1.88 反编译源码，以及对相关中转实现的代码分析。文中能够确认的是“客户端发送了什么、服务端因此理论上能拿到什么”。至于服务端实际如何判定风险、哪些字段权重更高，外部无法直接观测。下面所有推论，都会尽量停在这个边界之内。

---

## 一、问题不在某个字段，而在账号上下文

常见防封经验通常围绕几个单点展开：IP 是否稳定、地区是否一致、系统时区是否匹配、请求频率是否过高、同一账号是否多地登录。这些因素当然可能影响风险，但它们解释不了一个现象：有些用户基本照做，仍然遇到封号；也有些用户并不严格遵守，却长期正常使用。

这说明问题不太可能只落在某一个字段上。

更合理的看法是：服务端面对的不是一条条互不相关的请求，而是一组围绕账号持续积累的上下文。一次请求可能看起来很正常，但如果这个账号长期只表现为“不断产生推理请求”，却缺少真实客户端通常会伴随产生的设备、进程、配置和遥测信息，它在数据形态上就会显得不完整。

所以，与其问“这个 IP 会不会封号”“这个时区要不要改”，不如先问一个更基础的问题：

> 这个账号的使用痕迹，是否像一个真实客户端在一台真实机器上持续运行？

这不是一个玄学问题。Claude Code 客户端确实会发送足够多的上下文信息，使服务端有条件从“单次请求”之外观察账号的使用形态。

![[笔记同步助手/images/18454f52a927b261c3e04ae8095da274_MD5.png]]

**图 1：风险判断不只来自单个字段，而来自持续积累的账号上下文。**

---

## 二、每次推理请求里，都会带上本机上下文

Claude Code 发起 `/v1/messages` 请求时，请求体里不只有用户输入的内容。它还会在 `system` 和 `metadata` 中加入一组本机环境与账号相关的信息。

先看 `system`。客户端会把当前工作目录、是否处在 git 仓库中、操作系统平台、shell 类型、系统版本等信息拼进系统提示里。也就是说，服务端看到的请求并不是一段完全脱离环境的文本，而是一段带着运行环境的文本。

其中最容易被忽略的是路径和 git 信息。

当前工作目录通常是绝对路径。在 macOS 上，它可能长这样：

```
/Users/chen/project/acme-billing
```

这类路径里经常包含本机用户名、项目名、公司名或客户名。它未必是敏感内容，但它确实是一种稳定的身份和项目线索。

如果当前目录是 git 仓库，Claude Code 还会读取一部分 git 状态，包括当前分支、默认分支、最近提交、工作区状态，以及本地配置中的 `git user.name`。这里有两个细节值得单独说明。

第一，`git user.name` 很多人会填真实姓名或常用英文名。客户端会主动读取这个值，并把它作为 `Git user` 写进提示词。它不是网络层无意带出的信息，而是客户端在构造上下文时主动收集的内容。

第二，`git status --short` 会列出已修改和未跟踪的文件路径。文件内容不会因此直接上传，但文件名本身有时就包含项目代号、客户名、内部功能名，甚至 `.env.production` 这类不该轻易出现在上下文里的名称。换句话说，请求里可能不只有“你问了什么”，还会带上“你正在什么项目里、改哪些文件”。

顺手澄清一个常见说法：`git log --oneline` 本身只包含短 hash 和提交标题，不包含提交时间，也不包含时区。真正更值得注意的，反而是 `Git user` 和工作区文件路径。

除了 `system`，请求里的 `metadata.user_id` 也很关键。它大致由三部分组成：

```
{
"device_id": "本机生成并保存在配置里的 ID",
"account_uuid": "当前登录账号 ID",
"session_id": "当前会话 ID"
}
```

其中 `device_id` 来自本地 `～/.claude/config.json` 里的 `userID`。它是按本机配置保存的，不是每次登录都重新生成。只要这个配置文件还在，换一个 Claude 账号后，新账号仍可能带着同一个本地 `device_id` 发起请求。

这件事本身不等于“换号一定会被关联”，但它说明客户端具备把多个账号与同一台本机环境联系起来的基础条件。对于正常用户来说，这只是设备连续性的体现；对于频繁切换账号或批量使用账号的场景，它会成为更明显的上下文线索。

![[笔记同步助手/images/f9cb976492fed21b6480e6cfb2c57ea4_MD5.png]]

**图 2：一次推理请求并不只有用户输入，也会带上本机与账号上下文。**

---

## 三、客户端运行期间，还会持续发送遥测事件

如果说 `/v1/messages` 里的信息回答的是“这次请求从哪里来”，那么遥测事件回答的就是“这个客户端进程正在怎样运行”。

Claude Code 有一条第一方事件上报通道，端点是 `event_logging/batch`。默认情况下，只要客户端在产生事件，就会按批次上报。源码里可以看到默认导出间隔是 10 秒，单批最多 200 条事件。

这类事件会附带一组 `envContext`。它包含的不是某个单一字段，而是一整组运行环境画像：

| 分组 | 典型信息 | 含义 |
| --- | --- | --- |
| 系统 | 操作系统、CPU 架构、Linux 发行版、内核版本、WSL 版本 | 这台机器大致是什么环境 |
| 运行时 | Node 版本、是否 Bun、Claude Code 版本、构建时间 | 客户端以什么运行时启动 |
| 工具链 | 是否能找到 npm、yarn、pnpm、bun、deno、node | 本机 JavaScript 生态工具是否存在 |
| 终端与编辑器 | iTerm、tmux、VS Code、Cursor、Windsurf、JetBrains 系列 IDE 等 | 用户通常在哪类开发环境中使用 |
| 部署环境 | CI、GitHub Actions、Docker、Kubernetes、AWS、GCP、Azure、Cloudflare Pages 等 | 客户端更像跑在本地、容器、CI，还是云环境 |
| 版本控制 | 当前目录的 VCS 类型，以及远程仓库地址的哈希标识 | 当前工作目录与仓库的稳定线索 |

![[笔记同步助手/images/158e1ffadaf4b21701459568f41b39bc_MD5.png]]

**图 3：遥测事件让服务端看到一个持续运行的客户端进程。**

这里有几项尤其值得注意。

`deploymentEnvironment` 不只是读取环境变量。某些情况下，它还会通过文件系统特征识别运行位置，例如 Docker、云服务或特定 CI 环境。对真实开发者来说，这些信息通常只是环境描述；但如果一个账号长期表现为在云函数、容器或自动化环境中高频使用，它就会和普通本地使用产生差异。

`terminal` 也不只是粗略区分终端。它会识别一系列编辑器和 IDE，包括 Cursor、Windsurf、VS Code、VSCodium，以及 JetBrains 的多个产品。这类信息可以帮助服务端理解客户端运行在怎样的开发场景中。

更重要的是，遥测里还有进程指标，例如进程已运行时间、内存占用、堆内存使用量、CPU 使用率等。它们不像操作系统或 Node 版本那样是静态指纹，而更像一个正在运行的客户端进程的生命体征。

这就是单次请求和持续遥测之间的区别：请求头可以被精心还原，某些字段也可以被模拟；但一个真实进程在持续运行时自然产生的环境、时序和资源使用痕迹，更难被批量、长期、低成本地伪造。

还有一个细节是 `rh`。它是 git 远程仓库地址归一化后的 SHA256 前 16 位。它不暴露远程地址明文，但同一个仓库会稳定映射到同一个值。因此，服务端理论上可以用它识别“这些会话是否来自同一个仓库上下文”。

到这里，风险模型已经比较清楚了：Claude Code 的正常使用并不只是 `/v1/messages`。围绕推理请求，还会自然出现一组设备、进程、环境、仓库和会话上下文。

---

## 四、中转池账号的问题：请求像，但上下文不完整

这也是为什么中转池账号的风险不能只从“单次请求是否伪装得像”来理解。

以 sub2api 这类中转网关为例，它的基本模式是：运营方维护一批 Claude 订阅账号的 OAuth token，对外发放自己的 `sk-xxx`；用户本地 Claude Code 把请求发到中转网关，中转网关再用池子里的某个账号 token 转发给 Anthropic。

从单次 `/v1/messages` 看，这类实现可以做得非常接近真实 Claude Code。它会复刻 Claude Code 的消息指纹算法，计算 `x-anthropic-billing-header` 里的 `cc_version`；也会根据抓包结果还原 `cch` 客户端证明；甚至连 HTTP 头大小写这种细节都可能按真实客户端格式处理。

也就是说，如果只看一条推理请求，它未必粗糙。

真正的问题在请求之外。

典型中转使用方式大致是这样的：

```
export ANTHROPIC_BASE_URL=https://gateway.example.com
export ANTHROPIC_AUTH_TOKEN=sk-xxx
```

这个 `sk-xxx` 是中转服务发给用户的 key，不是 Anthropic 的 OAuth access token，也不是 Anthropic 官方 API key。池子账号真正的 OAuth token 留在中转服务器里，并没有进入用户本地的 Claude Code 进程。

于是就出现了一个结构性差异：本地 Claude Code 可以继续产生推理请求，但许多需要 OAuth 身份的伴随流量，不会以池子账号的身份从用户本机发出。

![[笔记同步助手/images/7c8a01cf522c50a8b411a62b12b55070_MD5.png]]

**图 4：中转池账号可以转发推理请求，但难以补齐账号绑定的伴随上下文。**

按 v2.1.88 源码逐项看，大致是这样的：

| 通道 | 正常订阅账号 | 典型中转池账号 |
| --- | --- | --- |
| `/v1/messages`推理请求 | 由本机 Claude Code 发出，带账号与设备上下文 | 经中转转发，可以被伪装得很像真实请求 |
| `event_logging/batch`遥测 | 可带账号身份上报，并与本机进程关联 | 本机可能会发遥测，但难以绑定到池子里的 OAuth 账号 |
| trusted devices | 登录或设备相关流程中出现 | 因本机没有池子账号 OAuth token，通常不会以该账号身份发出 |
| policy limits / profile / roles / usage | 依赖 first-party URL、OAuth 或订阅账号状态 | 典型中转 key 不满足这些调用条件 |
| bootstrap / 配置拉取 | 正常登录流程中可能出现 | 中转 key 通常不触发完整登录与配置链路 |

重点不是“sub2api 没有把某个请求头仿好”，而是池子账号的 OAuth token 没有被一个真实的本地 Claude Code 进程持有。它能产生大量看起来像真实客户端的推理请求，却不一定能产生同一个账号在真实客户端上应有的设备、遥测、配置和登录上下文。

从账号侧看，这种形态更接近一组缺少伴随上下文的推理流。它和普通用户的差异不是某一个字段异常，而是整体结构不完整。

这也解释了为什么“把请求做得很像”并不等于“账号就像真实用户”。前者解决的是单次请求的外观，后者需要一整套长期、连贯、能自洽的使用上下文。

---

## 五、如何理解“像真实用户”

前面几节都停留在 Claude Code 客户端层面。再往上，还可以提出一个更宽的判断框架：一个账号是否像真实用户，可能不只取决于 CLI，也可能取决于网页端、桌面端、手机 App 等多端行为是否整体连贯。

这里必须再次强调边界：本文没有逆向网页端和 App 端，也无法证明服务端一定会把这些跨端行为合并用于风控。下面只是基于账号系统和客户端上报机制得到的合理推断，用来帮助理解风险形状，而不是服务端判定规则。

真实用户的使用痕迹通常不是单一的。他可能在网页上问几句，在手机上查看回复，在桌面端继续对话，也可能在自己的电脑上用 Claude Code 写代码。这些行为未必频繁，也未必规律，但它们通常围绕同一个人的时间、设备、地理移动和工作习惯展开。

因此，风险不应该简单理解为“IP 变了就危险”或“固定 IP 就安全”。更准确的说法是：变化本身不是问题，变化是否符合一个人的现实活动轨迹才是问题。

手机网络切换、出差、从家里到公司、从 Wi-Fi 到蜂窝网络，这些都是真实用户很自然的变化。相反，同一账号短时间内跨越很远地区同时活跃，或者长期以无人值守的方式持续产生高频请求，就更难和普通人的使用方式对齐。

对个人真实用户来说，最稳妥的做法其实很朴素：用自己的账号，在自己的设备上，按正常工作节奏使用。不要为了“防封”把所有行为都压成单一 IP、单一设备、单一时间段的机械模式。真实用户本来就会留下自然的上下文。

对中转池和批量账号来说，问题则难得多。它们需要的不只是伪装一个请求，也不是伪装一个设备字段，而是为大量账号批量制造长期连贯的设备、进程、登录、遥测和跨端使用痕迹。这个成本远高于复刻某个请求头。

所以，判断风险时可以用一个更稳的标准：

> 这个账号的使用方式，是否像一个真实人在真实设备上持续、连贯地使用 Claude？

它不是保证安全的公式，但比单独盯着 IP、时区、请求头更接近问题本身。

---

## 结语

本文真正想说明的，并不是“某个字段会导致封号”，也不是“照着某份清单就能安全”。从客户端源码能看到的，是 Claude Code 在正常运行中会持续提供一组上下文：推理请求里的本机环境、git 状态和设备标识，遥测里的系统、终端、工具链、部署环境和进程指标，以及围绕账号产生的伴随流量。

这些信息让服务端有条件看到的不只是“这次请求是什么”，还可能包括“这个账号背后是否有一个真实客户端在持续运行”。

中转池账号更容易出问题，核心也在这里。它们可以把单次 `/v1/messages` 做得很像真实 Claude Code，但池子账号本身往往没有被用户本地的真实客户端持有，因此缺少与账号绑定的设备、遥测、登录和配置上下文。风险不是来自某一个没仿好的字段，而是来自整体使用形态的不完整。

最后仍然要把边界放在明处：我们能确认的是客户端发送了什么，服务端因此能够获得哪些判断材料；服务端实际怎么判、阈值在哪里，外部看不到。理解这些材料的价值，不在于获得一张保证安全的清单，而在于不再把注意力全部放在单个字段上。

---

## 证据索引

以下是文中涉及的主要源码位置，便于复核。正文只保留结论和必要解释，具体代码可按这些位置回看。

| 主题 | 位置 |
| --- | --- |
| `/v1/messages`请求体中挂载 `system` 和 `metadata` | `src/services/api/claude.ts:1699` |
| 系统提示中拼接工作目录、平台、shell、系统版本等环境信息 | `src/constants/prompts.ts:677` |
| git 分支、状态、最近提交、`git user.name` 的读取 | `src/context.ts:36` |
| `metadata.user_id`中的 `device_id`、`account_uuid`、`session_id` | `src/services/api/claude.ts:519` |
| 本地 `userID` 生成与保存 | `src/utils/config.ts:1757` |
| 第一方遥测批量上报间隔和批大小 | `src/services/analytics/firstPartyEventLogger.ts:300` |
| `envContext`中的平台、运行时、终端、部署环境等字段 | `src/services/analytics/metadata.ts:574` |
| 进程 uptime、内存、CPU 等指标 | `src/services/analytics/metadata.ts:648` |
| git 远程仓库哈希 `rh` | `src/services/analytics/metadata.ts:739` |
| HTTP 鉴权头构造 | `src/utils/http.ts:69` |
| trusted devices 调用条件 | `src/services/trustedDevice.ts:124` |
| policy limits 调用条件 | `src/services/policyLimits/index.ts:174` |
| sub2api 网关转发逻辑 | `backend/internal/service/gateway_service.go:45` |
| sub2api billing header 与消息指纹复刻 | `backend/internal/service/gateway_billing_block.go` |
| sub2api `cch` 客户端证明复刻 | `backend/internal/service/gateway_billing_header.go` |
| sub2api 上游账号设备标识处理 | `backend/internal/service/identity_service.go:215` |

---

## 参考资料

第一节中关于社区经验规则的归纳，整理自以下公开资料：

•Claude 封号机制深度调研：风控底层逻辑与 2026 年终极解决方案\[1\]•Claude 封号规则报告（小耳 Jane@X）\[2\]•Claude 封号潮来袭？2026 最新 Claude 封号原因解析与防封号策略\[3\]•Claude 防封指南：被封 3 次后总结的 3 大作死行为和 6 条防封经验\[4\]•claude 封号的核心原因：一张完整的“红线”清单（V2EX）\[5\]•Claude Code 防封号指南：国内终极解决方案（GitHubDaily）\[6\]•新开 Claude 账户被秒封，封号原因总结（SkyVPN）\[7\]•聊一聊 Claude 封号的原因以及防止 Claude 封号的小技巧（知乎）\[8\]

### References

`[1]` Claude 封号机制深度调研：风控底层逻辑与 2026 年终极解决方案:_https://gitcode.csdn.net/69cb448b54b52172bc65ad68.html_  
`[2]`Claude 封号规则报告（小耳 Jane@X）:_https://x.com/xiaoerzhan/status/2039176601536188888_  
`[3]`Claude 封号潮来袭？2026 最新 Claude 封号原因解析与防封号策略:_https://zhuanlan.zhihu.com/p/2019847407014293704_  
`[4]`Claude 防封指南：被封 3 次后总结的 3 大作死行为和 6 条防封经验:_https://zhuanlan.zhihu.com/p/1936013939122078205_  
`[5]`claude 封号的核心原因：一张完整的“红线”清单（V2EX）:_https://global.v2ex.co/t/1151272_  
`[6]`Claude Code 防封号指南：国内终极解决方案（GitHubDaily）:_https://github.com/GitHubDaily/GitHubDaily/issues/808_  
`[7]`新开 Claude 账户被秒封，封号原因总结（SkyVPN）:_https://skyvpn.me/blog/claude-account-banned-reasons_  
`[8]`聊一聊 Claude 封号的原因以及防止 Claude 封号的小技巧（知乎）: _https://zhuanlan.zhihu.com/p/2021634675228063097_

  

---

![[笔记同步助手/images/86cb16ba84a79a0d328786065a9629d3_MD5.jpg|cover_image]]

Original 执着的猫 猫的AI安全笔记

---

内容效果不满意？[点此反馈](https://feedback.notebooksyncer.com/feedback/07d70158_1780071491274?u=https%3A%2F%2Fmp.weixin.qq.com%2Fs%3F__biz%3DMzY4NjMwMTQzNw%3D%3D%26mid%3D2247483675%26idx%3D1%26sn%3Defb510ed7a6fecf2c9444dffa4c8d50b%26chksm%3Df2b3752697a3c93a3644a0c1a96fa1e127de8b7e16291d81a5253d12783a29d5d4d896c1dce5%26mpshare%3D1%26scene%3D1%26srcid%3D0530UICrR6eZAGlUGWUN41PP%26sharer_shareinfo%3D19524bef59f1ee23ab4e93810bcdac53%26sharer_shareinfo_first%3D95f9313d2434c706ee06c2c5ed99f818%23rd&s=obsidian)